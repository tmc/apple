//go:build darwin

// Package coremlruntime wraps private CoreML runtime entry points that are
// useful when working with compiled models, stateful prediction, and
// client-supplied output backings.
package coremlruntime
